events { worker_connections 1024; }
http {
    include /etc/nginx/templates/honeypot-tarpit-http.conf;
    server {
        listen 80;
        server_name localhost;
@INCLUDE@
        location / { return 200 'ok'; }
    }
}
