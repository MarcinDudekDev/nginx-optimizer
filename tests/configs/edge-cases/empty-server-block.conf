# Shape: edge-empty-server — a server block with no location, no root and no content directives.
# Provenance: crafted regression fixture
# Edge Case: Minimal/Empty Server Block
# Should be handled gracefully

server {
    listen 80;
    server_name empty.example.com;
}
