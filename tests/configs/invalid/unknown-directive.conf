# Provenance: crafted negative fixture
# NEGATIVE FIXTURE — this file MUST fail `nginx -t`. See invalid/README.md.
# Shape: invalid-unknown-directive — a plausible-looking directive that does not exist. Distinguishes
# "config is malformed" from "config needs a module we do not have" in the parser's error reporting.
server {
    listen 8087;
    server_name bogus.example.com;
    enable_turbo_mode on;
    location / { return 200 'ok'; }
}
