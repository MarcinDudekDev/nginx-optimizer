# Provenance: crafted negative fixture
# NEGATIVE FIXTURE — this file MUST fail `nginx -t`. See invalid/README.md.
# Shape: invalid-unbalanced-braces — the location block is never closed.
server {
    listen 8085;
    server_name broken.example.com;

    location / {
        try_files $uri $uri/ =404;
}
