# Provenance: crafted negative fixture
# NEGATIVE FIXTURE — this file MUST fail `nginx -t`. See invalid/README.md.
# Shape: invalid-missing-semicolon — `root` has no terminating semicolon, so nginx swallows the next
# directive as an argument.
server {
    listen 8086;
    server_name nosemi.example.com;
    root /var/www/nosemi
    index index.html;

    location / { try_files $uri $uri/ =404; }
}
