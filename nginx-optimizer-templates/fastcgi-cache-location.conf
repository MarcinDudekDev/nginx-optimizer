# FastCGI Cache - Location Context Directives
# Include this INSIDE the existing location ~ \.php$ block
# Requires $skip_cache variable from fastcgi-cache.conf (server context)
#
# NOTE: This file uses add_header, which triggers nginx's add_header
# inheritance quirk — all parent add_header directives are replaced.
# Security headers must be re-included in this location block.
# The optimizer's injection handles this automatically.

fastcgi_cache WORDPRESS;
fastcgi_cache_bypass $skip_cache;
fastcgi_no_cache $skip_cache;
add_header X-FastCGI-Cache $upstream_cache_status;
